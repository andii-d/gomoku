# Data pipeline
pandas
numpy
pyarrow          # Parquet support for pandas

# Calibration modelling
scikit-learn
joblib

# RenjuNet XML parsing
lxml

# Visualisation (board grid, calibration plots)
matplotlib

# Research environment
jupyterlab

# Testing
pytest

# Optional: richer CPU feature flags for picking the best Rapfi ISA build.
# Detection still works without it (via /proc/cpuinfo, sysctl, or CPUID).
py-cpuinfo