# Environment
.venv/
__pycache__/
*.pyc

# Rapfi engine binary and weights (download separately, GPL-3.0/CC0, not committed)
engine/rapfi_bin/

# RenjuNet data (private, offline use only per their terms, never commit)
data/renjunet/

# Generated data (large, regenerable — keep out of git; commit a small sample if useful)
data/raw/
data/processed/

# Trained models and plots (regenerable from data + training script)
calibration/models/
calibration/plots/

# Jupyter checkpoints
.ipynb_checkpoints/

# OS
.DS_Store